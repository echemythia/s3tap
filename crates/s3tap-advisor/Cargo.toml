[package]
name = "s3tap-advisor"
version.workspace = true
edition = "2021"
license.workspace = true
repository.workspace = true
description = "Optimization advisories over captured s3tap records (efficiency + caching)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
s3tap-schema = { path = "../s3tap-schema" }
s3tap-doctor = { path = "../s3tap-doctor" }
s3tap-replay = { path = "../s3tap-replay" }
